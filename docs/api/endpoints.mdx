# API Endpoints

## Create Project

`POST /projects`

## Update Panels

`POST /projects/{id}/panels`

## Calculate Energy

`POST /projects/{id}/energy`

## Generate Proposal

`POST /projects/{id}/proposal`
