# Lead & Project Management

### 1. Lead Capture

- **Validation**: Ensures the address is reachable via satellite.
- **Status Tracking**: Leads move from _New_ -> _Design_ -> _Proposal_ -> _Signed_.

### 2. Project Creation

- Every project gets a **Unique Project ID**.
- Stores utility bill data (Monthly kWh usage) to compare with solar production.
